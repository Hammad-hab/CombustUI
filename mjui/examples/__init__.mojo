from .imgViewer import *


fn runExample(name: String) raises:
    print('[WARNING]: Currently there is no support for resolving events in examples, so they might not work properly')
    if name == 'image_viewer_example':
        imgExample()